/**
 * @file
 * inpost behaviors.
 */
(function($, Drupal) {
    /**
     * Behavior description.
     */
    Drupal.behaviors.adstest = {

        attach: function attach(context, settings) {
            var e = document.createElement('div');
            e.id = 'qxEPubdrQIpk';
            e.style.display = 'none';
            document.body.appendChild(e);
        }
    };
})(jQuery, Drupal);